import React from 'react';

const Navbar = () => {
  return (
    <nav>
      <div>
        <h1>Recipes</h1>
        <div>
          <button>Login</button>
          <button>Signup</button>
        </div>
      </div>
    </nav>
  );
};

export default Navbar;
